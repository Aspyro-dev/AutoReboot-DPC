# AutoReboot-DPC

An Android Device Owner application designed to automatically reboot a device after it has remained locked for a configurable period of time.

## Current status

Development / prototype.

The current development version implements the lock-state detection and timer infrastructure. Automatic reboot functionality is not yet implemented.
